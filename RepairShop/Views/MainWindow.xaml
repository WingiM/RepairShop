<Window x:Class="RepairShop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RepairShop"
        mc:Ignorable="d"
        Title="Ремонтная мастерская" Height="450" Width="800">
    <Grid Background="{StaticResource PrimaryColor}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Background="{StaticResource SecondaryColor}" HorizontalAlignment="Stretch" Orientation="Horizontal">
            <Button x:Name="GoBackButton" Background="{StaticResource AccentColor}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" Click="GoBackButton_Click" Visibility="Collapsed">Назад</Button>
            <Image Margin="20 2 10 2" Source="/Images/tools.png" />
            <Label FontSize="18" VerticalAlignment="Center">Ремонтная мастерская</Label>
            <TextBlock Margin="20 0 0 0" x:Name="TitleTb" VerticalAlignment="Center" FontSize="18" HorizontalAlignment="Center"/>
        </StackPanel>
        <Frame x:Name="MainFrame" Grid.Row="1" Grid.Column="1"></Frame>
    </Grid>
</Window>
